import { css } from '@emotion/react'

export const cssPlaylist = css({
  position: 'relative',
  header: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    form: {
      maxWidth: 480,
      width: '100%',
    },
  },
})
